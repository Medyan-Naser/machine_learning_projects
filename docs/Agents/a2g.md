# A2G

## Overview
A2G is used here as a label for agent-to-agent / agent-group style systems.

In this docs section, the closest concrete implementation is AG2 (AutoGen), which is a framework for building multi-agent systems that collaborate via structured conversation.

See: `ag2-autogen.md`.
